<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Translator.TranslatePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Translator"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:Translator"
    NavigationCacheMode="Required"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:InvBoolConv
            x:Key="InvBoolConv" />
        <converters:BoolToOpacityConv
            x:Key="BoolToOpacityConv" />
        <converters:BoolToVisibilityConv
            x:Key="BoolToVisibilityConv" />
    </Page.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition
                Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition
                Height="Auto" />
            <RowDefinition
                Height="*" />
        </Grid.RowDefinitions>
 
        <StackPanel
            Grid.Row="0"
            Margin="10 10 10 0"
            Spacing="10">
            <TextBlock
                TextWrapping="WrapWholeWords"
                Text="Every translateable item in \Strings\en-US\Resources.resw is translated by the selected profile and saved to the other languages in \Strings.">
            </TextBlock>
            <StackPanel
                VerticalAlignment="Top"
                Orientation="Horizontal">

                <StackPanel
                    Spacing="5">
                    <StackPanel
                        Spacing="10"
                        VerticalAlignment="Center"
                        Orientation="Horizontal">
                        <Button
                            HorizontalAlignment="Center"
                            IsEnabled="{x:Bind Vm.IsBusy, Mode=OneWay, Converter={StaticResource InvBoolConv}}"
                            Command="{x:Bind Vm.StartTranslateCommand}">
                            <Button.Content>
                                <StackPanel
                                    Orientation="Horizontal"
                                    Spacing="10">
                                    <TextBlock
                                        Text="Translate" />
                                    <ProgressRing
                                        Width="16"
                                        Height="16"
                                        HorizontalAlignment="Center"
                                        IsActive="{x:Bind Vm.IsTranslating, Mode=OneWay}" />
                                </StackPanel>
                            </Button.Content>
                        </Button>
                        <Button
                            IsEnabled="{x:Bind Vm.IsTranslating, Mode=OneWay}"
                            Command="{x:Bind Vm.CancelTranslateCommand}"
                            Content="Cancel" />
                        <CheckBox
                            Padding="8, 0, 0, 0"
                            Margin="0"
                            Content="Save to cache"
                            IsEnabled="{x:Bind Vm.IsBusy, Mode=OneWay, Converter={StaticResource InvBoolConv}}"
                            IsChecked="{x:Bind Vm.TranslateSaveToCache, Mode=TwoWay}" 
                            VerticalContentAlignment="Center" />
                    </StackPanel>
                    <ProgressBar
                        Value="{x:Bind Vm.TranslateProgress, Mode=OneWay}"
                        Opacity="{x:Bind Vm.IsTranslating, Mode=OneWay, Converter={StaticResource BoolToOpacityConv}}"
                        Maximum="100"
                        Minimum="0">
                    </ProgressBar>
                </StackPanel>
            </StackPanel>
        </StackPanel>

    </Grid>
</Page>
